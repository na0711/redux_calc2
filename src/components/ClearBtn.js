import React from 'react';
// import PropTypes from 'prop-types';

const ClearBtn = ({n, onClick}) => (
  <button onClick={onClick}>clear</button>
);

// NumBtn.propTypes = {
//   onClick: PropTypes.func.isRequired,
// };

export default ClearBtn;